interface:
  display_name: "Gitcrawl"
  short_description: "Search local NexisClaw issue and PR history before live GitHub triage"
  default_prompt: "Use $gitcrawl to inspect NexisClaw issue and PR history, find related threads and duplicate candidates, then verify actionable decisions with live GitHub."
