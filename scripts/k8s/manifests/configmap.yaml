apiVersion: v1
kind: ConfigMap
metadata:
  name: NexisClaw-config
  labels:
    app: NexisClaw
data:
  NexisClaw.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.NexisClaw/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "NexisClaw Assistant",
            "workspace": "~/.NexisClaw/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # NexisClaw Assistant

    You are a helpful AI assistant running in Kubernetes.
